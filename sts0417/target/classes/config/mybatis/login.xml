<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">	
	
	<insert id="accountinsert">
		insert into account values(#{id}, #{password}, #{email}, #{phone}, to_date(#{birthday},'yyyymmdd'), #{gender}, #{location}, 1, sysdate)
	</insert>
	
	<select id="loginselect" resultType="String">
		select id from account where id=#{id} and password=#{password}
	</select>
	
</mapper>







